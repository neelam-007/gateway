# Resource bundle for CancelSecurityContextPropertiesDialog

label.fail-session-not-found=&Fail if the secure conversation session does not exist
label.fail-session-expired=Fail if the secure conversation session is &expired
label.permit-when=&Permit cancellation

permit-cancellation.NONE = always
permit-cancellation.USER = when the token is owned by an authenticated user
permit-cancellation.TOKEN = when the token was used to authenticate
