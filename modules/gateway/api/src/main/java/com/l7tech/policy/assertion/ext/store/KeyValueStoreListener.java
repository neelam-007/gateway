package com.l7tech.policy.assertion.ext.store;

import com.l7tech.policy.assertion.ext.CustomListener;

/**
 * A tagging class that all key value store listeners must extend.
 */
public abstract class KeyValueStoreListener<Callback> implements CustomListener<Callback> {
}