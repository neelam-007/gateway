# Resources for ListenerEditPanel

label.header = Enter the IP address and port for the HTTPS listener.

addr = IP Address
port = Port

PatternValidator = ${label} should be a valid address.
ListenerEditPanel$LocalIpAddressValidator = ${label} is not available on this server.
