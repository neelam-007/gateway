package com.ca.apim.gateway.extension.event;

/**
 * Events fired by the Gateway implements this interface and could be listened by any extension module that implements a proper {@link EventListener}.
 */
public interface Event {
    // marker interface only
}
