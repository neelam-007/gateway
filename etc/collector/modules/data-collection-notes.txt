devices:
	cat /proc/meminfo
	lspci -tv
	cat /proc/cpuinfo
	ls -Rl /dev
	dmidecode

filesystems:
	df
	cat /proc/mdstat
	mdadm --detail -scan
	mount
	lsblk
	pvdisplay
	vgdisplay
	lvdisplay
	cat /etc/mdadm.conf
	mdadm --query --detail DISK_NAMES
	mdadm --query --detail PARTITION_NAMES
	mdadm --examine DISK_NAMES
	mdadm --examine PARTITION_NAMES

gateway:
	rpm -qa ssg ssg-appliance ssem
	ls -l $GW_DEFAULT/etc/conf/omp.dat
	cat $GW_DEFAULT/etc/conf/node.properties
	cat $GW_DEFAULT/etc/conf/system.properties
	cat $GW_DEFAULT/etc/conf/ssglog.properties
	cat $GW_DEFAULT/var/logs/ssg_0_0.log
	cat $PC_DEFAULT/config/ssgsysconfig.log
	jstack -l $GWPID

java:
	$GW_ROOT/JDK/bin/java -version
	cat /$GW_ROOT/JDK/jre/lib/security/java.security
	ls -l /opt/SecureSpan/JDK/jre/lib/ext

kernel:
	 cat /proc/version
	 lsmod
	 cat /etc/sysctl.conf
	 cat /proc/cmdline

monitor:
	ps axjf
	vmstat 1 30
	timeout 30s netstat -i 1
	ps auxwww | grep gateway

mysql:
	service mysql status
	mysql ssg -e 'select * from ssg_version'
	mysql ssg -e 'show tables'
	mysql ssg -e 'show slave status'
	mysql ssg -e 'show master status'
	cat /etc/my.cnf

network:
	ifconfig -a
	netstat -nr
	iptables -nL
	iptable -v -nL

os:
	cat /etc/redhat-release
	chkconfig --list
	rpm -qa
	cat /var/log/messages
	who -r
	dmesg
	cat /etc/ntp.conf
	ntpq -p
	cat /etc/security/limits.conf
	cat /etc/security/limits.d/*ssg*
	whoami
	ulimit -S -a
	ulimit -H -a

vmware:
	vmware-checkvm
	vmware-checkvm -hp
	vmware-toolbox-cmd stat balloon
	vmware-toolbox-cmd stat memlimit
	vmware-toolbox-cmd stat memres
	vmware-toolbox-cmd stat cpures
	vmware-toolbox-cmd stat cpulimit
	vmware-toolbox-cmd timesync status
