#
# host.properties file for use with process controller on appliance
#

#
host.type = appliance

# Platform common options
host.jre = /opt/SecureSpan/JDK

# Platform specific options
host.tarari = true
host.sca = true

# 
host.nodes = default
